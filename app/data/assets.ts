// Story 1 – Vosje Bakt Brood
// Export these images from Figma and place them in public/images/
const base = import.meta.env.BASE_URL;

const coverVosje = `${base}images/cover-vosje.png`;
const iconSun = `${base}images/icon-sun.png`;
const iconKnife = `${base}images/icon-knife.png`;
const iconOven = `${base}images/icon-oven.png`;
const iconBread = `${base}images/icon-bread.png`;

// Story 2 – Pingu op het Strand
const coverPingu = `${base}images/cover-pingu.png`;
const iconTowel = `${base}images/icon-towel.png`;
const iconUmbrella = `${base}images/icon-umbrella.png`;
const iconSunglasses = `${base}images/icon-sunglasses.png`;
const iconCrab = `${base}images/icon-crab.png`;

export {
  coverVosje,
  iconSun,
  iconKnife,
  iconOven,
  iconBread,
  coverPingu,
  iconTowel,
  iconUmbrella,
  iconSunglasses,
  iconCrab,
};
